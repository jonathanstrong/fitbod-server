#![allow(unused_imports)]

use chrono::prelude::*;
use uuid::Uuid;
use sqlx::Pool;
use sqlx::postgres::Postgres;
use crypto::hmac::Hmac;
use crypto::sha2::Sha256;

fn main() {
    println!("Hello, world!");
}
